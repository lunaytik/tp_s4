{% extends 'base.html.twig' %}

{% block title %}Formulaire Adresse{% endblock %}

{% block body %}
<h1>Formulaire Adresse</h1>

{{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-primary">Créer</button>
{{ form_end(form) }}
{% endblock %}
